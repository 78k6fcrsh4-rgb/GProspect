# GProspect — runtime dependency
#
# The app runs from the self-contained demo entry point (main.py) with a
# single command:
#
#     streamlit run main.py
#
# No API keys, no database, and no cryptographic secrets are required.
# "Run Grant Search" and "Search Archival (990)" call the free, keyless
# Grants.gov and ProPublica Nonprofit Explorer public APIs over HTTPS.
streamlit>=1.31.0
requests>=2.31.0

# Optional: only used by the "✨ Enhance with AI" button on the email draft.
# That button only appears if you set your own ANTHROPIC_API_KEY environment
# variable — nothing in this app requires it, and it's never hardcoded here.
anthropic>=0.40.0
