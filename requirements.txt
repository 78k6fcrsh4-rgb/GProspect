# GProspect — runtime dependencies
# Derived from actual imports across portal/, agent/, tools/, scoring/, learning/, output/.

# Web portal (FastAPI)
fastapi>=0.111.0
uvicorn[standard]>=0.30.0
python-multipart>=0.0.9          # required by OAuth2PasswordRequestForm

# Database
sqlalchemy>=2.0.30
# Postgres driver — installed for Phase 3+. SQLite still works for tests
# + zero-config local dev; Postgres becomes the canonical production target
# when DATABASE_URL is set to a postgresql:// or postgresql+psycopg:// URL.
psycopg[binary]>=3.2.0

# Config / validation
python-dotenv>=1.0.1
pydantic[email]>=2.7.0           # EmailStr

# Auth
python-jose[cryptography]>=3.3.0 # JWT
passlib[bcrypt]>=1.7.4           # password hashing
# bcrypt 4.1+ raises on >72-byte secrets which breaks passlib's internal
# detect_wrap_bug() probe. Pin <4.1 until passlib publishes a fix.
bcrypt>=4.0.0,<4.1.0
slowapi>=0.1.9                   # rate limiting (login brute-force defense)

# Agent / data sources
anthropic>=0.34.0                # AI scoring + web search
requests>=2.31.0                 # grants.gov, IRS 990

# Export
openpyxl>=3.1.2                  # Excel export

# Doc-assist (Phase 1a intake — text extraction from uploaded files)
mammoth>=1.6.0                   # .docx → plain text
pypdf>=4.0.0                     # .pdf → plain text

# Digest export (Phase 1b — weekly digest PDF + DOCX)
python-docx>=1.1.0               # DOCX writing
reportlab>=4.0.0                 # PDF writing

# Test runner (used by tests/ — not loaded at runtime by the portal)
pytest>=8.0.0
httpx>=0.27.0                    # FastAPI TestClient backend
